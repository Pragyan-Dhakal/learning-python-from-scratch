a=float(input("Enter a first number: "))
b=float(input("Enter a second numbe: "))

#hc=round(a+b) #It will round up to nearest integer.

c=round(a+b,2) #It will round up.

print(f"The sum is {c}")#It will print coma in a english way.

#If you type python and name of file in terminal it will print the result
#If you code the name of file then it will create a file.